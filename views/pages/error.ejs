<% if (!req.get("X-PJAX")) {%>
    <%- include('../partials/header'), {
        account : account
    } %>
<%} %>

        <div class="page_name"><span><%= error_name %></span></div>

        <div class="welcome_text_box" style="left: 295px; top: 80px; text-align: center;">
            <span style="text-align: center; display: block; left: 0; top: 195px;"><%= error_description %></span>
        </div>